[project]
name = "signature-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "gkatrilakas", email = "katrilakas@hotmail.gr" }
]
requires-python = ">=3.14"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.5",
    "psycopg[binary]>=3.2.0",
    "streamlit>=1.61.1",
]

[project.scripts]
signature-app = "signature_app:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
